import 'package:flutter/material.dart';

const difficultyColors = <Color>[
  Color(0xFF30D407), // light
  Color(0xFFEBE109), // soft
  Color(0xFFEF4040), // hard
];

/// Brand seed color — drives the full ColorScheme for both light and dark themes.
const seedColor = Color(0xFFFF5A5F);
